{
  "name": "smart-barcode-billing",
  "private": true,
  "scripts": {
    "start": "concurrently -n api,web -c blue,green \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && node src/index.js",
    "start:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
